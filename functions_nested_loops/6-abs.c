#include "main.h"

/**
 * _abs - checking alphabetic caracters
 * Description: Whrite a function that computes
 * the absolute value of an integer
 * @r: The character to check
 * Return: r if >= 0 , else -r
*/
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
